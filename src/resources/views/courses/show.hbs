<div class="mt-4">
    <div class="row">
        <div class="col-log-3">
            <h2>Course: {{courseDetail.name}}</h2>
            <img src="{{courseDetail.image}}" class="card-img-top" alt="{{courseDetail.name}}">
            <p class="card-text">{{courseDetail.description}}</p>

        </div>
    </div>
</div>